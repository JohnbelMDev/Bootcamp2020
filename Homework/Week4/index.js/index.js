// setting varibale to equal to zero
var i=0;
// setting variable equal to smaller than 7
while(i<7){
//charater is set to an empty string
    // var chara= '';
    for(var z=0;z<=i; z++){
      var chara= '';
    }
    for(var q=0; q<=i;q++){
      // chara+="*";
      chara+="*";
    }
    //putting outside the while so for it to update
  console.log(chara);
  i++;

  }
